local placeId = game.PlaceId

local supported = {
    [11729688377] = "loadstring(game:HttpGet('https://raw.githubusercontent.com/Tubris/public-shit-/refs/heads/main/BoogaBooga'))()",
    [2317712696] = "loadstring(game:HttpGet('https://raw.githubusercontent.com/Tubris/public-shit-/refs/heads/main/WildWestMe'))()",
    [11879754496] = "loadstring(game:HttpGet('https://raw.githubusercontent.com/Tubris/public-shit-/refs/heads/main/BoogaBooga'))()",
}

local discordInviteCode = "SatFXKfdHa"
local discordUrl = "https://discord.gg/" .. discordInviteCode

local function copyToClipboard(text)
    if setclipboard then
        setclipboard(text)
        return true
    elseif toclipboard then
        toclipboard(text)
        return true
    else
        return pcall(function()
            game:GetService("GuiService"):SetClipboard(text)
        end)
    end
end

if supported[placeId] then
    loadstring(supported[placeId])()
else
    local player = game.Players.LocalPlayer
    local playerGui = player:WaitForChild("PlayerGui")

    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "ChubbisonInvalid"
    screenGui.ResetOnSpawn = false
    screenGui.Parent = playerGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 460, 0, 250)
    mainFrame.Position = UDim2.new(0.5, -230, 0.5, -125)
    mainFrame.BackgroundColor3 = Color3.fromRGB(25, 8, 40)
    mainFrame.BorderSizePixel = 0
    mainFrame.ClipsDescendants = true
    mainFrame.Parent = screenGui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 16)
    corner.Parent = mainFrame

    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(45, 15, 70)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 5, 35))
    })
    gradient.Parent = mainFrame

    local titleFrame = Instance.new("Frame")
    titleFrame.Size = UDim2.new(1, 0, 0, 55)
    titleFrame.BackgroundColor3 = Color3.fromRGB(60, 20, 85)
    titleFrame.BorderSizePixel = 0
    titleFrame.Parent = mainFrame

    local titleCorner = Instance.new("UICorner")
    titleCorner.CornerRadius = UDim.new(0, 16)
    titleCorner.Parent = titleFrame

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 1, 0)
    title.Text = "⚠️ CHUBBISON INVALID ME ⚠️"
    title.TextColor3 = Color3.new(1, 1, 1)
    title.BackgroundTransparency = 1
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 22
    title.TextStrokeTransparency = 0.5
    title.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    title.Parent = titleFrame

    local message = Instance.new("TextLabel")
    message.Size = UDim2.new(1, -40, 0, 65)
    message.Position = UDim2.new(0, 20, 0, 75)
    message.Text = "This script does not support the current game.\nJoin our Discord for updates & supported games:"
    message.TextColor3 = Color3.fromRGB(210, 180, 240)
    message.BackgroundTransparency = 1
    message.Font = Enum.Font.SourceSansSemibold
    message.TextSize = 15
    message.TextWrapped = true
    message.TextXAlignment = Enum.TextXAlignment.Center
    message.Parent = mainFrame

    local discordButton = Instance.new("TextButton")
    discordButton.Size = UDim2.new(0, 280, 0, 48)
    discordButton.Position = UDim2.new(0.5, -140, 0, 155)
    discordButton.Text = "📋 JOIN DISCORD"
    discordButton.TextColor3 = Color3.new(1, 1, 1)
    discordButton.BackgroundColor3 = Color3.fromRGB(35, 170, 70)
    discordButton.Font = Enum.Font.GothamBold
    discordButton.TextSize = 18
    discordButton.BorderSizePixel = 0
    discordButton.Parent = mainFrame

    local buttonCorner = Instance.new("UICorner")
    buttonCorner.CornerRadius = UDim.new(0, 10)
    buttonCorner.Parent = discordButton

    local statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, -40, 0, 30)
    statusLabel.Position = UDim2.new(0, 20, 0, 215)
    statusLabel.Text = ""
    statusLabel.TextColor3 = Color3.new(0.6, 1, 0.6)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Font = Enum.Font.SourceSans
    statusLabel.TextSize = 13
    statusLabel.TextXAlignment = Enum.TextXAlignment.Center
    statusLabel.Parent = mainFrame

    local closeButton = Instance.new("TextButton")
    closeButton.Size = UDim2.new(0, 38, 0, 38)
    closeButton.Position = UDim2.new(1, -48, 0, 8)
    closeButton.Text = "X"
    closeButton.TextColor3 = Color3.new(1, 1, 1)
    closeButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
    closeButton.Font = Enum.Font.GothamBold
    closeButton.TextSize = 20
    closeButton.BorderSizePixel = 0
    closeButton.Parent = mainFrame

    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 10)
    closeCorner.Parent = closeButton

    discordButton.MouseButton1Click:Connect(function()
        if copyToClipboard(discordUrl) then
            statusLabel.Text = "✅ Copied to clipboard!"
            statusLabel.TextColor3 = Color3.new(0.5, 1, 0.5)
        else
            statusLabel.Text = "❌ Copy failed. Manual link: " .. discordUrl
            statusLabel.TextColor3 = Color3.new(1, 0.5, 0.5)
        end
        task.delay(4, function()
            if statusLabel then statusLabel.Text = "" end
        end)
    end)

    closeButton.MouseButton1Click:Connect(function()
        screenGui:Destroy()
    end)

    local dragging, dragStart, startPos = false
    mainFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = mainFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    mainFrame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
            local delta = input.Position - dragStart
            mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end
